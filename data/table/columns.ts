import { GridColDef } from '@mui/x-data-grid';

export const sharedColumnSettings: GridColDef | any = {
  flex: 1,
  headerAlign: 'center',
  align: 'center',
  sortable: false,
  disableColumnMenu: true,
  minWidth: 80,
};
